# startup-webpack-only-js

## mode of use

# clone
    git clone https://github.com/rbb-soft/startup-webpack-only-js.git

# install
    npm install

# transpile and run devServer
    npm run dev

# info
working directory is SRC

the directory of the final result (bundles) is BUILD

the directory of the final result (bundles) is BUILD

# enjoy! 
